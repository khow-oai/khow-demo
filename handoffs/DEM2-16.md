# DEM2-16 Evidence Handoff

This draft PR is a delivery-evidence PR created through the GitHub app fallback path because local push of the validated branch was blocked by PushPatrol. It does not contain the full local code patch.

## Ticket

- Linear: https://linear.app/openai/issue/DEM2-16/codex-demo-harden-release-triage-handoff
- Local repo branch: `codex/demo-system-pr`
- Local commit carrying the full patch: `70d5251 harden release triage report flow`

## Goal And Private Diagnostics

- Goal loop inspected repo rules, README, the conductor skill, ticket, tests, private diagnostics, and presenter runbook.
- Tunnel MCP runtime selection was attempted through the Tunnel MCP plugin.
- Tunnel runtime was blocked because no trusted `tunnel-client` binary was available from explicit path, `TUNNEL_CLIENT_BIN`, `.tunnel-client-bin`, adjacent plugin output, or PATH.
- Private diagnostics used the demo-safe local synthetic MCP smoke artifact, not a live tunnel result.
- Synthetic diagnostics for `triage-console`: `status=degraded`, `needs_escalation=true`, signal includes `report endpoint should be security-reviewed before handoff`.

## Security Result

Controlled finding: report path traversal in `/reports?name=...`.

- Baseline artifact: `artifacts/security/baseline-reproducer.json` had `read_private_marker=true` before remediation.
- Fix: local patch resolves the candidate report path and requires it to remain under `wow_demo/reports` and be a regular file.
- Remediation artifact: `artifacts/security/remediation-reproducer.json` has `read_private_marker=false` and `FileNotFoundError`.

## Validation Commands

```bash
python3 -m unittest discover -s tests -v
python3 scripts/private_mcp_smoke.py
python3 scripts/security_reproducer.py blocked
python3 scripts/browser_validate.py http://127.0.0.1:8765
sh scripts/dry_run.sh
```

Observed locally: all passed; `sh scripts/dry_run.sh` ended with `dry run: ok`.

## Browser Validation

Codex Browser opened Mission Control at `http://127.0.0.1:8765`, clicked `Open the report Security will harden`, and captured local proof:

- `artifacts/screenshots/release-triage-dashboard-live.png`
- `artifacts/screenshots/release-triage-report-live.png`

## Fallback Artifact Pack

Primary local evidence paths:

- `artifacts/evidence/goal-run.md`
- `artifacts/evidence/security-plugin-handoff.md`
- `docs/evidence-ledger.md`
- `artifacts/fallbacks/tunnel-runtime-status.md`

## Residual Risk And Reviewer Focus

- This PR is delivery evidence only because local PushPatrol blocked publishing the full patch branch.
- Review the local diff from commit `70d5251` for the actual code change.
- Tunnel MCP remains fallback-labeled until a trusted `tunnel-client` binary is selected.
- Private diagnostics are synthetic demo-safe data; a real tunneled diagnostics surface would need normal authn/authz controls.
